# 测试清单（v19.8.12 窗口版 / v19.9.9 卡片版）

## 0. 准备

- [ ] 两个脚本都更新到新版本：窗口版 **19.8.12**、卡片版 **19.9.9**（Tampermonkey / ScriptCat 都更新）
- [ ] **两个脚本不要同时启用**，否则划词会出现两个按钮
- [ ] 按 F12 打开控制台，用过滤框输入 `[划词翻译]`，出问题时把带这个前缀的日志/报错发我
- [ ] 每次报告请写清：**哪个分支 + 哪个网站 + 什么操作 + 看到什么 + 控制台日志**

---

## 1. 窗口版（v19.8.12）

本轮在 v19.8.11（已确认好用的版本）基础上，新增「**跨网页共用同一条对话链**」，并保留原有全部行为。

### 1.1 测哪些网站

| 类型 | 站点建议 |
|---|---|
| 普通新闻 | 新浪 / 网易 / 腾讯新闻、BBC 中文 |
| 百科文档 | 百度百科、维基百科、MDN、GitHub 的 README 页 |
| 社区论坛 | 知乎、贴吧、Reddit、B 站评论区 |
| 脚本注入敏感 | **YouTube**、Google 搜索、Gmail、X（原 Twitter） |
| 富文本 | 微信读书、语雀 / 飞书文档 |
| 特殊情况 | Chrome 内置 PDF 预览、本地 HTML 文件 |

### 1.2 要执行的操作（原有行为回归）

- [ ] 划选一段 → 点按钮 → 窗口出现在鼠标旁 → 自动发送 → 输出译文 → 窗口长高
- [ ] **连续划词 3~5 次（每次换不同段落）**：不新开窗口、窗口移动到你鼠标旁、缩回小缝再展开，并且**每一次都要真的发送并输出译文**
- [ ] 同一段文字连续点两次按钮：不应出现「一句话翻两遍」
- [ ] 划词后**不点按钮**、直接点页面别处：按钮应消失
- [ ] 划词 → 点按钮 → 立刻点回原页面：窗口应该**跳到最上层**并移动到你鼠标旁
- [ ] **对话链是否连续**：连续翻译几段后，DS 窗口里应看到每次问答依次追加在**同一个对话**里
- [ ] **手动关掉 DS 窗口 → 再划词**：窗口应重新打开并**回到之前的对话**，且这次也要**成功发送、出译文**
- [ ] 高度检查：译文最后一行与窗口底边应留有余量
- [ ] 划词所在页面刷新后再划词：应能接管已有窗口而不是越开越多
- [ ] 划很长一段（几百字）：窗口到 480px 封顶后应内部滚动
- [ ] 划词后滚动页面：按钮应消失
- [ ] 分别用中英混排、含代码块、含链接的段落各试一次

### 1.3 本轮重点：跨网页共用对话链（v19.8.12 新增）

> 核心预期：**不管在哪个网页划词，都回到同一条对话链**，且任意时刻只保留一个翻译窗口。

- [ ] **A 站翻译 → 切到 B 站（不同域名）翻译**：DS 窗口里应看到两段问答接在**同一个对话**里，而不是 B 站另起一条新对话
- [ ] **A/B/C 三个不同网站轮流翻译**：始终只有**一个**翻译 DS 窗口，不会越开越多
- [ ] 在 B 站划词时，若 A 站曾开过窗口：窗口应**被带到最前**（或至少移动到你鼠标旁）并接着发送
- [ ] 若发现「**旧窗口自动关闭、新窗口打开**」：属预期（浏览器不允许跨站复用同名窗口时的接管行为），只要新窗口仍回到**同一条对话**就算正常
- [ ] 跨网页往返翻译 5 次以上：**不应出现同一句话被发两遍**（本轮加了任务认领去重）
- [ ] 完全关掉所有 DS 窗口 → 等 10 秒 → 在任意网页划词：应重新打开并**回到之前的对话**发送
- [ ] 手动在 DS 窗口里点了「新对话」→ 回到网页再划词：应停在这个新对话继续（而不是跳回旧对话）

### 1.4 重点观察

- [ ] 窗口初始是「一条缝」，不是一打开就一个大窗
- [ ] 侧边栏 / 顶栏等杂项是否被隐藏（不应看到完整 DS 网页）
- [ ] **控制台是否出现 `ReferenceError`**（这类致命错误本轮重点排查）
- [ ] 观察 DS 窗口地址栏是否只在末尾多出 `#ds-worker-数字`（说明走的是同文档导航，且被正确识别为翻译窗口）；如果窗口**重新加载**了（出现加载动画、对话被清空），请告诉我
- [ ] 控制台里可留意 `[划词翻译]` 日志：跨网页时应出现「复用 DeepSeek 窗口…」；被接管时旧窗口出现「翻译窗口已被更新的窗口接管，本窗口自动关闭」

---

## 2. 卡片版（v19.9.9）

本轮重点是 Trusted Types 兼容、扫描防抖、以及回传稳定性。

### 2.1 测哪些网站

| 类型 | 站点建议 |
|---|---|
| 普通站（对照） | 新闻、维基、MDN 各一个 |
| **Trusted Types 站** | **YouTube**（最重要）、Google 搜索、Gmail |
| 多标签页 | 任意两个不同域名的网站（A 站 + B 站） |
| 富文本 | 微信读书、语雀 |

### 2.2 要执行的操作

- [ ] 首次划词 → 点按钮 → 卡片在鼠标旁出现并显示「翻译中…」；标签栏出现标题为 **翻译助手** 的标签页
- [ ] 译文流式进入卡片（状态文字消失），卡片高度跟着长高
- [ ] **第二次及以后的翻译**：卡片应先保持「翻译中…」，**不应先显示上一次的译文**再跳成当前结果（本轮修复点）
- [ ] **连续划词 3~5 次**：标签栏不应不断冒出新的「翻译助手」
- [ ] **跨标签页复用**：在 A 网站翻译 → 打开 B 网站再翻译 → 不新开工作页
- [ ] **B 网站的流式颗粒度**应与 A 网站一致（不应一段一段地跳）
- [ ] 拖动卡片左上角握把 → 拖后位置固定，后续译文增长不再跳回鼠标旁
- [ ] 点 ✕ 关闭卡片 → 再划词应能正常弹新卡片
- [ ] 卡片还在时再划词 → 新任务应刷新卡片，而不是叠加
- [ ] 不让 DS 登录 / 断开登录，等 60 秒：应提示「等待超时：请确认 DeepSeek 已登录」
- [ ] 翻译最后一次后干等 3 分钟：工作标签页应自动关闭

### 2.3 重点观察（本轮修复项）

- [ ] **YouTube 上卡片能否显示译文**：正常显示富文本最好；若显示为无格式纯文本，也算修复成功（预期内的降级）
- [ ] YouTube 上控制台**不应**出现 `TypeError` / Trusted Types 相关报错
- [ ] 拖动手柄：应显示为 6 个小圆点（不再是可能变方框的特殊字符）
- [ ] 标签页标题「翻译助手」应正常显示，无方框乱码
- [ ] 长译文流式期间页面应保持流畅（本轮给 MutationObserver 加了防抖）

---

## 3. 已知待确认（不用急着修，帮我留意现象即可）

- [ ] **思考过程**仍可能偶发泄漏（本轮只优化了性能，没动过滤逻辑）
- [ ] 首次使用会短暂在前台闪现「翻译助手」标签页（架构决定的已知现象）
- [ ] 窗口版复用时 `focus()` 可能被浏览器忽略，窗口会移动但不一定跳到最前

---

## 4. 反馈模板

```
分支：窗口版 / 卡片版
版本：19.8.12 / 19.9.9
网站：
浏览器：
操作步骤：
实际现象：
控制台日志（[划词翻译] 前缀）：
```
